\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{verifyagents_fallback}[2026/08/23 Local preview fallback for NeurIPS 2026]
\RequirePackage[letterpaper,top=0.75in,bottom=0.75in,left=0.75in,right=0.75in]{geometry}
% Use lmodern (always available) instead of times+courier which need extra TFMs
\RequirePackage{lmodern}
\RequirePackage{natbib}
\RequirePackage{fancyhdr}
\RequirePackage{xcolor}
\setlength{\columnsep}{0.25in}
\setlength{\parindent}{1em}
\setlength{\parskip}{0pt}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0pt}
\makeatletter
\renewcommand{\maketitle}{%
  \twocolumn[\@maketitle]\thispagestyle{fancy}%
}
\renewcommand{\@maketitle}{%
  \newpage\null\vskip 0.15in%
  \begin{center}%
    {\LARGE\bfseries \@title \par}%
    \vskip 0.12in%
    {\large \@author \par}%
  \end{center}%
  \vskip 0.18in%
}
\makeatother
\renewenvironment{abstract}{%
  \centerline{\large\bfseries Abstract}\vspace{0.5ex}%
  \begin{quote}\small
}{\end{quote}\vspace{0.5ex}}
\AtBeginDocument{\small}
